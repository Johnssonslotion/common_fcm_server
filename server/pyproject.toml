[tool.poetry]
name = "fcm-server"
version = "0.1.0"
description = "fcm basic functions"
authors = ["johnpark <johnpark@baithings.co.kr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
